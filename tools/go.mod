// Stdlib only, on purpose: these run in CI and in a console developer's checkout,
// and neither should have to fetch anything to regenerate a table.
module github.com/mirusu400/DaeMoon/tools

go 1.22
